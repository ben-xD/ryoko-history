// Put examples here that should throw eslint errors. This allows us to catch that they are failing.
// To test them, we'd need to temporarily symlink or copy the file into `src` of a project. Alternatively, we'd need to add that file to a project, by adding the file path to the prettier command, tsconfig file and eslint config file of that app, which is more complicated.

// Node examples, e.g. NO useEffect linting errors, tailwind errors